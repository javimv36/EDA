#include <iostream>
#include <fstream>
#include "Stack.h"

using namespace std;

int main() {

	int a[10];
	char numero[10];

	cin >> numero[10];
	cout << numero[1];
	
	/*Stack<int>* pila = new Stack<int>();
	cin >> numero;
	
	while (i != ' ') {
		
	
	}
	while (numero != -1) {
		for (int i = 0; i < sizeof(a); i++) {
			a[i] = numero;
			pila->push(a[i]);
		}
		pila->sumaPila();
		cin >> numero;
	}
	delete pila;*/
	return 0;
}